[tool.ruff]
line-length = 120
target-version = "py312"
# evals/evaluation.py and evals/connections.py are vendored (Anthropic mcp-builder, Apache 2.0)
extend-exclude = [".venv", "evals/evaluation.py", "evals/connections.py"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "W", "I", "B", "UP"]
# The codebase consistently uses timezone.utc; datetime.UTC adds no clarity here.
ignore = ["UP017"]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
